# spacecraft-inventory-system
A React props exercise that builds a spacecraft inventory management system. Displays inventory items with their name, type, quantity, and price, and uses conditional rendering to show warnings for low-stock items and high-value items exceeding a set threshold.
